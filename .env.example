# --- Supabase (optionnel) ---
# Sans ces variables, l'application tourne en mode démo (mock en mémoire).
# Renseignez-les pour activer Supabase (Auth + Postgres + Storage).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# --- TMDB (V2 - import automatique des films) ---
TMDB_API_KEY=
